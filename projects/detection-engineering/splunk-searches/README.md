# Splunk Searches

This folder contains the Splunk Search Processing Language (SPL) queries used throughout the Detection Engineering Program.

Each search corresponds to a documented detection located in the `detections` folder and represents the detection logic used to identify specific security events.

As the project grows, additional searches will be added to support new detections, validation, and alerting workflows.